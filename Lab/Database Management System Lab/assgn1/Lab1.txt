SECTION A
1)
CREATE TABLE EMPLOYEE(
EMP_NO VARCHAR2(10),
EMP_NAME VARCHAR2(100),
EMP_STREET VARCHAR2(100),
EMP_CITY VARCHAR2(50),
EMP_JOB VARCHAR2(50),
EMP_HIREDATE DATE,
EMP_MGR VARCHAR2(10),
EMP_DEPTNO VARCHAR2(10)
)
CREATE TABLE DEPT(
DEPT_ID VARCHAR2(10),
D_NAME VARCHAR2(80),
D_LOCATION VARCHAR2(50)
)
2)
ALTER TABLE EMPLOYEE ADD (EMP_SALARY NUMBER, EMP_DATEOFBIRTH DATE)
3)
ALTER TABLE EMPLOYEE RENAME COLUMN EMP_DATEOFBIRTH TO EMP_DOB
4)
ALTER TABLE EMPLOYEE DROP COLUMN EMP_DOB
5)
ALTER TABLE DEPT RENAME TO REMDEPT
6)
ALTER TABLE EMPLOYEE RENAME COLUMN EMP_CITY TO EMP_LOC

SECTION B
CREATE TABLE EMPWITHC(
EMP_ID NUMBER CONSTRAINT EMP_ID_PK PRIMARY KEY,
EMP_NAME VARCHAR2(20) CONSTRAINT EMP_NE_NN NOT NULL,
STREET VARCHAR2(20),
CITY VARCHAR2(20) CONSTRAINT EMP_CY_UQ UNIQUE,
AGE NUMBER CONSTRAINT EMP_AE_CK CHECK (AGE>=18)
)
1)
ALTER TABLE EMPWITHC MODIFY EMP_NAME VARCHAR2(125)
2)
ALTER TABLE EMPWITHC ADD CONSTRAINT EMPC_EN_UQ UNIQUE(EMP_NAME)





ROUGH DATA
CREATE TABLE EMPLOYEE(
EMP_NO VARCHAR2(10),
EMP_NAME VARCHAR2(100),
EMP_STREET VARCHAR2(100),
EMP_CITY VARCHAR2(50),
EMP_JOB VARCHAR2(50),
EMP_HIREDATE DATE,
EMP_MGR VARCHAR2(10),
EMP_DEPTNO VARCHAR2(10)
)

DROP TABLE EMPLOYEE
DESC EMPLOYEE

CREATE TABLE DEPT(
DEPT_ID VARCHAR2(10),
D_NAME VARCHAR2(80),
D_LOCATION VARCHAR2(50)
)

DROP TABLE DEPT
DROP TABLE EMP

SELECT TABLE_NAME FROM ALL_TABLES

ALTER TABLE EMPLOYEE ADD (EMP_SALARY NUMBER, EMP_DATEOFBIRTH DATE)

DESC EMPLOYEE

ALTER TABLE EMPLOYEE RENAME COLUMN EMP_DATEOFBIRTH TO EMP_DOB

ALTER TABLE EMPLOYEE DROP COLUMN EMP_DOB

ALTER TABLE DEPT RENAME TO REMDEPT


DESC REMDEPT

ALTER TABLE EMPLOYEE RENAME COLUMN EMP_CITY TO EMP_LOC

DESC EMPLOYEE

CREATE TABLE EMPWITHC(
EMP_ID NUMBER CONSTRAINT EMP_ID_PK PRIMARY KEY,
EMP_NAME VARCHAR2(20) CONSTRAINT EMP_NE_NN NOT NULL,
STREET VARCHAR2(20),
CITY VARCHAR2(20) CONSTRAINT EMP_CY_UQ UNIQUE,
AGE NUMBER CONSTRAINT EMP_AE_CK CHECK (AGE>=18)
)

ALTER TABLE EMPWITHC MODIFY EMP_NAME VARCHAR2(125)

ALTER TABLE EMPWITHC ADD CONSTRAINT EMPC_EN_UQ UNIQUE(EMP_NAME)

DROP TABLE EMPWITHC
DESC EMPWITHC
